{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the KeyHint scaffold window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
